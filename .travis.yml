language: csharp
dist: trusty
mono: none
dotnet: 2.1.2

install:
  - dotnet restore
  - dotnet restore squadup/
  - dotnet restore squadup.Tests/

script:
  - dotnet build
  - dotnet build squadup/
  - dotnet test squadup.Tests/
